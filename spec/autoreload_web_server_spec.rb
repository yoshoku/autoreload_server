# frozen_string_literal: true

RSpec.describe AutoreloadWebServer do
  it "has a version number" do
    expect(AutoreloadWebServer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
